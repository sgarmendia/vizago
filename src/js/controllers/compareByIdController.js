function compareByIdController($scope, $routeParams) {
    
  const timeStamp = $routeParams.id

  console.log(timeStamp)








    // $scope.getCompare = () => {

    //     vizagoService.getAnalysis( )
    //         .then( console.log )

    // }
}

module.exports = compareByIdController